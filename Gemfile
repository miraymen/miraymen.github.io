source "https://rubygems.org"

# Matches the GitHub Pages build environment for local previews
# (run with `bundle exec jekyll serve`).
gem "github-pages", group: :jekyll_plugins

gem "webrick", "~> 1.8"
